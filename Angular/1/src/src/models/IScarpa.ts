export interface IScarpa {
    colore: string,
    disponibile: boolean,
    foto: HTMLImageElement | string,
    id: number,
    nome: string,
    prezzo: number,
    quantita: number,
    taglia: number | string,
}